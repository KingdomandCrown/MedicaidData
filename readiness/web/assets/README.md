# Brand assets

Drop the **real** MinervaAI mark here as `minerva-mark.png` (or .svg — update the
`src` in `../index.html`).

The build spec is explicit: use the actual gold owl logo assets. Do not
approximate, redraw, or substitute a generated mark. Until the file is present
the page falls back to a plain wordmark rather than showing a stand-in owl.
