# Runtime dependencies. See pyproject.toml for the authoritative list.
requests>=2.28
SQLAlchemy>=2.0
ijson>=3.2
openpyxl>=3.1

# Optional: install to load into PostgreSQL instead of SQLite.
# psycopg[binary]>=3.1

# Dev/test:
# pytest>=7.0
